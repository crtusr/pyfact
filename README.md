A simple Quoting and payment tracking system made on 2023 that works on the same database as TETE

At the moment the database fields are hardcoded in facturador.py since this was made to accelerate price lookup and calculation. It was not conceived as a standalone project.
